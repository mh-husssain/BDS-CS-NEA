/*
// Selects HTML form element using querySelector, assiging it to form.
const form = document.querySelector('#formReg');

form.addEventListener('submit', (event) => { // This script runs when the form is submitted
    event.preventDefault(); 
    inputFields.style.display = 'none';
    success.innerHTML = '<h1>Form submitted successfully.</h1>';
    setTimeout(function() {
      event.target.submit();
      }, 3000);
});

//
*/